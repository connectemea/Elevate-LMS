# create once
git checkout -b dev
git push -u origin dev

# new work
git checkout -b feat/dashboard-layout
# work → commit → push
git add .
git commit -m "feat: build dashboard layout"
git push -u origin feat/dashboard-layout

# merge PR to dev
# test on dev branch

# finally, merge dev → main for release
git checkout main
git merge dev
git push origin main
